[One]
rank = 1
desc = assigning variable cheat code
commands = [
	"${WINIMG}=Echo /tmp/firefox.png",
	"${PLAY}=Echo /tmp/play.pat",
	"${PAUSE}=Echo /tmp/pause.pat",
	"${SKIP}=Echo /tmp/skip-ad.pat",
	"${THEATER}=Echo /tmp/theater.pat",
	]


[Two]
rank = 2
desc = assigning variable cheat code
commands = [
	"${WINIMG}=Echo /tmp/firefox.png",
	"${PLAY}=Echo /tmp/play.pat",
	"${PAUSE}=Echo /tmp/pause.pat",
	"${SKIP}=Echo /tmp/skip-ad.pat",
	"${THEATER}=Echo /tmp/theater.pat",
	]


[Launch email client]
rank = 3
desc = launches thunderbird from ubuntu taskbar
commands = [
	"${PATTERN}=Echo /tmp/mail.pat",
	"${SCREENSHOT}=Echo /tmp/screen",
	"${FTP_SERVER}=Echo 10.150.124.93",
	"Ftp download ${FTP_SERVER} anonymous anonymous ${PATTERN} mail.pat",
	"Desktop screenshot ${SCREENSHOT}",
	"Desktop mouseclick 0 ${SCREENSHOT} ${PATTERN}"
	]